# Private sources – local template

Keep this file local. Do not commit populated paths or credentials.

```yaml
team_vault_handoff:
codex_session_jsonl:
claude_session_jsonl:
dataflow_health_endpoint:
lms_repo_clone:
tutor_repo_clone:
credentials_location:
```

The public handoff is sufficient for visual and documentation work. Private sources are required only for participant-context backend implementation.
